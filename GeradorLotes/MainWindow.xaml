<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="GeradorLotes.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GeradorLotes"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Gerador de Lotes">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <!--  TitleBar  -->
            <RowDefinition Height="*" />
            <!--  NavigationView  -->
        </Grid.RowDefinitions>

        <TitleBar
            x:Name="TitleBar"
            BackRequested="TitleBar_BackRequested"
            IsBackButtonVisible="{x:Bind quadroRaiz.CanGoBack, Mode=OneWay}"
            IsPaneToggleButtonVisible="True"
            PaneToggleRequested="TitleBar_PaneToggleRequested"
            Title="Gerador de Lotes de Tarefas"
            Subtitle="VERSÃO EM DESENVOLVIMENTO">

            <TitleBar.LeftHeader>
                <ImageIcon
                    Height="16"
                    Margin="8,0,8,0"
                    Source="/Assets/AppIcon.ico" />
            </TitleBar.LeftHeader>

            <TitleBar.Content>
                <AutoSuggestBox
                    Width="320"
                    VerticalAlignment="Center"
                    PlaceholderText="Pesquisar.."
                    QueryIcon="Find" />
            </TitleBar.Content>

        </TitleBar>

        <NavigationView 
            x:Name="NavigationViewPadrao" 
            Grid.Row="1" 
            IsBackButtonVisible="Collapsed"
            IsPaneToggleButtonVisible="False"
            IsSettingsVisible="False"  
            Loaded="OnNavigationViewControlLoaded" 
            SelectionChanged="OnNavigationViewSelectionChanged">
            
            <Frame x:Name="quadroRaiz" />
            <NavigationView.MenuItems>
                <NavigationViewItem x:Name="TransferenciasItem" Content="Transferencias" Tag="GeradorLotes.Views.PaginaTransferencias" />
            </NavigationView.MenuItems>
        </NavigationView>
    </Grid>
</Window>
